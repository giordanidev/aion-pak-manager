name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Generate icons
        run: npm run icon

      - name: Build app bundle
        run: npm run build:app

      - name: Write version.json
        if: startsWith(github.ref, 'refs/tags/')
        run: node scripts/write-version-json.mjs ${{ github.ref_name }}

      - name: Package (setup + portable)
        run: npx electron-builder --win --publish never

      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: aion-pak-manager-windows
          if-no-files-found: ignore
          path: |
            release/*.exe
            release/*.blockmap
            release/latest*.yml

      - name: Publish to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          fail_on_unmatched_files: false
          files: |
            release/*.exe
            release/*.blockmap
            release/latest*.yml
            version.json

  linux-ubuntu:
    runs-on: ubuntu-latest
    env:
      LINUX_FLAVOR: linux-ubuntu
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Generate icons
        run: npm run icon

      - name: Build app bundle
        run: npm run build:app

      - name: Package (portable AppImage + .deb)
        run: npx electron-builder --linux AppImage deb --publish never

      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: aion-pak-manager-linux-ubuntu
          if-no-files-found: ignore
          path: |
            release/*.AppImage
            release/*.deb
            release/*.blockmap
            release/latest*.yml

      - name: Publish to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          fail_on_unmatched_files: false
          files: |
            release/*.AppImage
            release/*.deb
            release/*.blockmap
            release/latest*.yml

  linux-fedora:
    runs-on: ubuntu-latest
    container: fedora:latest
    env:
      LINUX_FLAVOR: linux-fedora
    steps:
      - name: Install toolchain
        # libxcrypt-compat provides libcrypt.so.1, required by the bundled
        # ruby used by electron-builder's fpm to produce the .rpm.
        run: dnf install -y nodejs npm git tar xz which findutils libxcrypt-compat

      - uses: actions/checkout@v4

      - run: npm ci

      - name: Generate icons
        run: npm run icon

      - name: Build app bundle
        run: npm run build:app

      - name: Package (.rpm)
        run: npx electron-builder --linux rpm --publish never

      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: aion-pak-manager-linux-fedora
          if-no-files-found: ignore
          path: |
            release/*.rpm
            release/latest*.yml

      - name: Publish to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          # No channel file here: electron-updater on Linux only supports the
          # AppImage channel, published by the ubuntu job as latest-linux.yml.
          fail_on_unmatched_files: false
          files: |
            release/*.rpm
